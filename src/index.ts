import { Alert, ConfirmOptions } from "./Service";

export { Alert };
export type { ConfirmOptions };

import { Alerts, AlertRenderProps, ConfirmRenderProps } from "./Component";

export { Alerts };
export type { AlertRenderProps, ConfirmRenderProps };