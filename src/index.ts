import { AlertRenderProps, ConfirmComponentHost, ConfirmRenderProps } from "./Component";
import { AlertOptions, AlertSeverity, ChooseOptions, ConfirmOptions, ConfirmService, Option } from "./Service";

export {
    ConfirmService,
    ConfirmComponentHost,
};
export type {
    AlertOptions,
    AlertSeverity,
    ChooseOptions,
    ConfirmOptions,
    AlertRenderProps,
    ConfirmRenderProps,
    Option,
};